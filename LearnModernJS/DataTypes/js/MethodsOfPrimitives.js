`use strict`;

const str = `Привет, мир!`;
const strMeth = str.toUpperCase();
alert(`Примитив "${str}", используя метод примитива, получили "${strMeth}".`);

const num = 1.12342354;
const numFix = num.toFixed(2);
alert(`Примитив "${num}", используя метод примитива, получили "${numFix}".`);
